#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
cd "$ROOT_DIR"

if command -v uv >/dev/null 2>&1; then
  PYTHONPATH=. uv run python tools/privacy_guard.py --staged
else
  PYTHONPATH=. python3 tools/privacy_guard.py --staged
fi
